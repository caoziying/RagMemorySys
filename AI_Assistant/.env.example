# ============================================================
# AI_Assistant 环境变量配置
# 复制此文件为 .env 并填入实际值
# ============================================================

# ── LLM API（与 RAG_Memory 共用同一 API 接入点）──────────────
MY_API_KEY=your_api_key_here
MY_API_BASE=https://api.chat.csu.edu.cn/v1
MY_MODEL=deepseek-v3-thinking

# ── RAG_Memory 服务地址 ───────────────────────────────────────
# 本地运行 RAG_Memory 时：
RAG_MEMORY_URL=http://localhost:8000
# Docker 容器内访问宿主机（当 RAG_Memory 也在宿主机运行时）：
# RAG_MEMORY_URL=http://host.docker.internal:8000
# 同一 Docker Compose 网络（需手动合并网络或使用 external network）：
# RAG_MEMORY_URL=http://rag-api:8000

# ── JWT 密钥（生产环境请替换为强随机字符串）─────────────────
SECRET_KEY=change-this-to-a-strong-random-secret-in-production
